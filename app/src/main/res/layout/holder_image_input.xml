<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Вопрос задания -->
    <TextView
        android:id="@+id/questionText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textStyle="bold"
        tools:text="Что изображено на рисунке?" />

    <!-- Изображение задания -->
    <ImageView
        android:id="@+id/taskImage"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:scaleType="centerCrop"
        android:contentDescription="@string/task_image"
        android:layout_marginTop="8dp"
        android:clickable="true"
        tools:src="@drawable/ic_image_placeholder" />

    <!-- Поле ввода с кнопками -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="12dp">

        <EditText
            android:id="@+id/inputField"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="@string/enter_answer"
            android:inputType="text"
            android:maxLines="1" />

        <ImageButton
            android:id="@+id/hintButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_hint"
            android:contentDescription="@string/show_hint"
            android:visibility="gone" />

        <ImageButton
            android:id="@+id/submitButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_check"
            android:contentDescription="@string/submit_answer" />
    </LinearLayout>

    <!-- Текст подсказки (изначально скрыт) -->
    <TextView
        android:id="@+id/hintText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:visibility="gone"
        android:textColor="?attr/colorPrimary"
        android:textSize="14sp"
        tools:text="Пример подсказки" />

    <!-- Текст результата (изначально скрыт) -->
    <TextView
        android:id="@+id/resultText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:visibility="gone"
        android:textSize="16sp" />

</LinearLayout>